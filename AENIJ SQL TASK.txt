1)SELECT customer.FIRSTNAME,customer.LASTNAME,sales.SALESPRICE, product.PRD_NAME FROM customer JOIN sales ON sales.CUST_ID=customer.CUSTOMERID JOIN product ON product.PRD_ID=sales.PRD_ID  WHERE sales.SALESDATE LIKE '2022%08%'
2)SELECT customer.CUSTOMERID,sales.CUST_ID, sales.PRD_ID,customer.FIRSTNAME,customer.LASTNAME FROM customer LEFT OUTER JOIN sales ON sales.CUST_ID=customer.CUSTOMERID WHERE sales.CUST_ID IS NULL
3)SELECT SUM(product.RECOMMENDED_PRICE ) AS PURCHASETOTAL,  SUM(sales.SALESPRICE) AS SALESTOTAL, customer.FIRSTNAME,customer.LASTNAME,sales.SALESPRICE,product.RECOMMENDED_PRICE FROM customer JOIN sales ON sales.CUST_ID=customer.CUSTOMERID JOIN product ON product.PRD_ID=sales.PRD_ID
4)DELIMITER //
 CREATE PROCEDURE INSCUSTDATA(INOUT FIRSTNAME VARCHAR(20),LASTNAME VARCHAR(20),CITY VARCHAR(20),STATE VARCHAR(20),ZIPVARCHAR(20))
       BEGIN
                  INSERT INTO customer(FIRSTNAME,LASTNAME,CITY,STATE,ZIP)VALUES(FIRSTNAME,LASTNAME,CITY,STATE,ZIP);
       END //
  DELIMITER ;
5)SELECT COUNT(SALES.PRD_ID), AVG(SALESPRICE),CATEGORY FROM PRODUCT JOIN SALES ON SALES.PRD_ID=PRODUCT.PRD_ID GROUP BY SALES.PRD_ID 
6)UPDATE SALES SET SALES.SALESPRICE = PRODUCT.RECOMMENDED_PRICE WHERE SALESDATE between '2022-03-10' AND '2022-03-20'
